# Local Competition Paper Update Notes

## Recommended Module Name

Redundancy-aware Local Opacity Competition

## Method Text

在复杂度引导的四叉树采样之后，来自不同输入视角的候选高斯被统一变换到同一个世界坐标系。为了抑制多视角重叠带来的冗余，我们在融合后的 Gaussian set 上引入 redundancy-aware local opacity competition。该模块不改变高斯的位置、尺度和颜色，也不执行物理删除；它只对透明度施加一个可解释的软门控。

具体地，对每个高斯 \(G_i\)，我们首先根据预测相机位姿、内参和可见像素足迹估计 resolution-aware local radius。当像素足迹不可用时，使用预测 scale 得到的局部半径作为 fallback。随后在局部邻域中计算 color-aware redundancy score \(R_i\)：空间邻近且颜色相近的高斯会被计入冗余，颜色明显不同的邻近高斯不会被计入。所有高斯的冗余分数通过

\[
T=\max(\operatorname{mean}(R)+\lambda\operatorname{std}(R), R_{\min})
\]

得到全局阈值。仅当 \(R_i>T\) 时，高斯被视为冗余候选，并由 \(R_i\) 归一化得到 redundancy coefficient \(\rho_i\)。最终透明度门控为

\[
\alpha_i'=\alpha_i \cdot g_i,
\]

其中 \(g_i\) 由 \(\rho_i\)、competition strength 和 minimum gate 共同决定。该设计对应代码中的 `redundancy_score -> redundancy_coef -> competition_gate -> opacity *= gate`，不包含跨视角 group softmax、位置合并或硬 prune。

## Ablation Interpretation

四叉树采样和 local opacity competition 的作用阶段不同。四叉树采样直接决定 Gaussian head 的候选分布，因此关闭该模块会明显改变采样位置、渲染质量和高斯数量。`random_equal_sample` 说明性能并不来自简单减少采样数，而来自复杂区域优先的空间分配；`pure_all_gaussians` 说明全量高斯会带来严重冗余。

Local competition 更像训练期透明度选择先验。它在训练中压低高冗余高斯的 opacity，使这些高斯更容易低于 `0.05` 的有效高斯阈值，并与 alpha/sparsity regularization 一起塑造 Gaussian head 的透明度输出偏好。由于 Gaussian head 的 token 已经包含多视角信息，这种偏好会在训练后被内化；因此仅在推理期关闭 local competition 时，PSNR/SSIM/LPIPS 的变化可能不显著，但 active Gaussian count、opacity mass 和 PLY size 仍然会揭示它对冗余透明度分布的影响。

## Figure Mapping

- Figure 1: main pipeline, shown as quadtree allocation plus local opacity suppression.
- Figure 2: quadtree sampling assets from `scripts/export_cp_flowchart_assets.py`.
- Figure 3: local competition assets from `new_local_competition/`, including local radius, color-aware neighbors, threshold-to-gate curve, and opacity before/after.
- Figure 4: opacity histogram/CDF with the `0.05` threshold, generated by `--save_gaussian_diagnostics` or the flowchart scripts.
- Figure 5: quality versus active Gaussian count / PLY size, from the updated ablation summaries.
- Figure 6: Gaussian map qualitative crops comparing dense/voxelized baselines against ours.

## Reproducible Commands

Generate local competition paper figures:

```bash
MPLCONFIGDIR=/tmp/matplotlib-cache /home/liuwei/anaconda3/envs/pi3-liuwei/bin/python3.10 \
  scripts/export_cp_local_competition_paper_figs.py \
  --model_impl _10 \
  --ckpt outputs/pi3_3dgs10_hunyuan_decoder/ckpts/best_model/model.safetensors
```

Generate full flowchart assets:

```bash
MPLCONFIGDIR=/tmp/matplotlib-cache /home/liuwei/anaconda3/envs/pi3-liuwei/bin/python3.10 \
  scripts/export_cp_flowchart_assets.py \
  --model_impl _10 \
  --ckpt outputs/pi3_3dgs10_hunyuan_decoder/ckpts/best_model/model.safetensors
```

Run the LC-on/LC-off 10k training-path comparison:

```bash
/home/liuwei/anaconda3/envs/pi3-liuwei/bin/python3.10 \
  scripts/run_pi3_3dgs10_lc_training_path.py \
  --gpus 0 \
  --stage all
```

Add per-Gaussian diagnostics to an ablation eval:

```bash
/home/liuwei/anaconda3/envs/pi3-liuwei/bin/python3.10 \
  scripts/run_pi3_3dgs8_ablation.py \
  --model_impl _10 \
  --save_gaussian_diagnostics \
  --datasets bicycle stump \
  --variants full no_local_no_opacity_filter
```
