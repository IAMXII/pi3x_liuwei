# @package _global_

trainer: trainers.pi3_trainer.Pi3Trainer

train:
  image_num_range: [2, 4]     # hres
  max_img_per_gpu: 4             # hres

  # low resolution
  resolution: 
    - [224, 224]

model:
  ckpt: null # <path-to-your-pretrain-checkpoint>