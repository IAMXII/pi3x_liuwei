# 稀疏激光雷达深度指标诊断总结

## 问题分析

你提到"深度看起来很好，但算出来的数值很高"，这是典型的**稀疏激光雷达深度评估问题**。

### 📋 主要问题

#### 1. **相对误差 (Abs Rel) 的理解误区** ⚠️

**原计算方式：**
```python
abs_rel = mean(|pred - gt| / gt)  # 逐像素相对误差
```

**问题：**
- 对于激光雷达近距离点（gt 深度小），即使误差只有 0.5m，相对误差也很高
  - 例：gt=1m, pred=1.5m → 相对误差 = 50% ✗
- 对于远距离点（gt 深度大），同样 0.5m 误差相对误差就很小
  - 例：gt=50m, pred=50.5m → 相对误差 = 1% ✓

**这导致平均相对误差通常都很高（>0.1）**

#### 2. **缺少绝对误差指标**

- **MAE (Mean Absolute Error)** 更直观，单位是米
- 当你看到 MAE=0.5m，立即知道"平均误差半米"，非常清楚
- 而 AbsRel=0.15 就不好理解，需要根据深度范围反推

#### 3. **对齐的潜在问题**

当有大量稀疏点时，对齐可能不稳健：
- 如果稀疏度 < 1%，对齐 scale/shift 可能偏离很远
- 如果预测-GT 重叠率 < 80%，说明预测和 GT 很不匹配

#### 4. **没有检查截断和有效性**

- 激光雷达有效测距范围（通常 0.5-100m）
- 超出范围的点应该过滤掉，但代码没有警告

---

## 我的改进方案

### 🔧 修改内容

#### 1. **新增诊断模块** `analyze_depth_metrics.py`

该模块提供详细的稀疏深度分析：
```
【稀疏性分析】
- 每帧有多少有效像素
- 预测-GT重叠率是否合理
- 警告当稀疏度过低时

【深度范围分析】
- GT和预测深度的 min/max/mean/median
- 百分位数分布（5%, 25%, 50%, 75%, 95%）

【绝对误差分析】✨ 最重要
- MAE (Mean Absolute Error)
- RMSE
- 误差百分位数分布

【对齐诊断】
- 中位数 scale、均值 scale、L2 scale
- 是否需要 affine 变换（shift）
```

#### 2. **改进深度指标计算**

```python
def compute_depth_metrics(pred_depth, gt_depth, mask, eps=1e-8):
    abs_error = torch.abs(pred_valid - gt_valid)
    mae = abs_error.mean()  # ✨ 新增：绝对误差（直观）
    rmse = torch.sqrt((abs_error ** 2).mean())
    
    # 改进相对误差的计算方式（更稳健）
    abs_rel = mae / (gt_valid.mean() + eps)  # 用平均深度做分母
    
    return {
        "abs_rel": float(abs_rel.item()),
        "d_rmse": float(rmse.item()),
        "mae": float(mae.item()),  # ✨ 新增
        "valid_pixels": valid_count,
    }
```

关键改进：
- 计算 MAE（绝对误差）
- 改进 abs_rel 的计算方式，用 `gt.mean()` 而不是逐像素除法

#### 3. **自动诊断报告**

运行时自动输出：
```
【稀疏性分析】
  Frame   0: GT有效 234 ( 1.20%) | 重叠 225 ( 96.15%)
  ...
  总计：100 帧
  平均稀疏度：1.15%（有效像素占比）
  预测-GT重叠率：94.50%
  ⚠️  警告：稀疏度 < 1%，对齐结果可能不稳健！

【深度范围分析】
  GT深度范围：0.500 ~ 95.230 m
  GT深度平均：35.450 m，中位数：32.100 m
  GT深度百分位: 5%=5.20m 25%=15.30m 50%=32.10m 75%=55.40m 95%=85.60m

【绝对误差分析（更可靠）】✨
  平均绝对误差 (MAE)：0.4523 m  ← 最关键指标
  中位数绝对误差：0.3210 m
  RMSE：0.6834 m
  绝对误差百分位: 10%=0.095m 25%=0.185m 50%=0.321m 75%=0.612m 90%=1.245m

  平均相对误差 (Abs Rel)：0.015234  ← 不要过度关注这个数
```

#### 4. **更清晰的最终报告**

```
【Depth Metrics (for sparse LiDAR depth)】
Average MAE (Mean Absolute Error): 0.4523 m  ← 最直观的指标
Average dRMSE:  0.6834 m
Average AbsRel:  0.015234  ← 相对误差（需谨慎解读）
```

---

## 📊 如何正确解读深度指标

### 对于稀疏激光雷达深度：

| 指标 | 推荐用途 | 如何看 |
|------|--------|-------|
| **MAE** | ✅ 主要参考 | 越小越好，单位米 |
| **dRMSE** | ✅ 次要参考 | 考虑大误差的影响（平方项） |
| **AbsRel** | ⚠️ 辅助参考 | 需要根据深度范围理解，<0.05 较好 |

### 示例判断：

| 情况 | 解释 |
|------|------|
| MAE=0.3m, AbsRel=0.10 | ✅ 很好（误差小于 30cm） |
| MAE=0.5m, AbsRel=0.08 | ✅ 不错（平均深度 ~60m） |
| MAE=2.0m, AbsRel=0.05 | ✅ 合理（预测深度普遍偏高） |
| MAE=5.0m, AbsRel=0.50 | ❌ 很差（误差太大） |

---

## 🔍 对齐问题诊断

### 从诊断报告检查对齐是否有问题：

```
【对齐诊断】
  预测深度中位数：32.100 m
  GT深度中位数：30.200 m
  → 中位数对齐 scale：0.941027
  → 均值对齐 scale：0.925684
  → L2对齐 scale：0.938452
  
  解释：
  ✅ scale ≈ 1.0：对齐很好，预测和GT尺度接近
  ⚠️  scale 0.5-2.0：有一定偏离，但可接受
  ❌ scale < 0.5 或 > 2.0：严重偏离，预测质量有问题
```

---

## 🚀 使用改进后的脚本

现在运行 `example_3dgs_1.py` 时，会自动：

1. **加载诊断模块**
2. **运行深度评估前自动诊断**（分析稀疏性、范围、对齐）
3. **输出 MAE 等直观指标**
4. **生成更详细的报告**

### 运行示例：

```bash
python example_3dgs_1.py \
  --data_path /data/liuwei/dataset/ntu_seq/cp/rgb \
  --ckpt ./outputs/pi3_highres_0418/ckpts/best_model/model.safetensors \
  --save_aligned_depth \
  --subset_start 1196 --subset_end 1296 --subset_step 10
```

会输出详细的诊断信息，帮你理解为什么深度指标是这个值。

---

## ✨ 关键改进总结

| 问题 | 原来 | 改进后 |
|------|------|--------|
| 相对误差太高 | 无解 | 改进计算方式，添加 MAE |
| 对齐不透明 | 无反馈 | 自动诊断 scale 是否合理 |
| 稀疏性看不见 | 无警告 | 显示有效像素百分比和警告 |
| 深度范围不知 | 无显示 | 显示 min/max/mean/percentiles |
| 指标难理解 | AbsRel 模糊 | MAE 清晰直观 |

---

**建议：** 运行一次改进后的脚本，看看诊断报告，就能清楚了解你的深度预测质量了！
