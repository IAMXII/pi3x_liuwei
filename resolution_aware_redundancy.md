# 分辨率感知的跨视角高斯冗余调节

为了更稳定地判断局部重复程度，我们首先为每个高斯估计一个分辨率感知的局部邻域尺度。受 Reduced-3DGS 中 pixel footprint 估计思想启发，对于第 $$i$$ 个高斯，我们根据其在可见输入视角中的投影关系估计一个局部 cube size $$q_i$$，用于近似该位置处一个图像像素所对应的三维空间尺度。随后通过全局缩放系数 $$\eta$$ 控制邻域大小，并将局部 cube 的半对角线作为该高斯的空间搜索半径：

$$
d_i = \frac{\sqrt{3}}{2}\,\eta q_i .
$$

其中，$$\eta$$ 对应 pixel scale，用于调节冗余检测邻域的松紧程度。与使用固定空间阈值不同，$$d_i$$ 是与高斯位置和相机观测分辨率相关的局部尺度：靠近相机或投影分辨率较高的位置具有更小的邻域半径，而远离相机或图像分辨率较低的位置则允许更大的局部检测范围。这样可以避免用同一个固定半径同时处理近景细节和远景平面，从而使冗余判断更加符合图像观测分辨率。

设第 $$i$$ 个高斯的三维位置、颜色和输入视角来源分别为 $$\mathbf{x}_i$$、$$\mathbf{c}_i$$ 和 $$s_i$$。我们首先根据空间距离和颜色相似性定义其局部候选集合：

$$
\widetilde{\mathcal{N}}_i =
\left\{
j\neq i \mid
\|\mathbf{x}_i-\mathbf{x}_j\| < d_i
\;\land\;
\|\mathbf{c}_i-\mathbf{c}_j\| < \delta_c
\right\}.
$$

为了避免将同一输入视角内部由于采样密度较高、纹理边界或局部几何细节产生的高斯误判为跨视角冗余，我们进一步引入视角来源约束。只有当局部候选集合中查找到的高斯与中心高斯共同构成跨视角重复组，即这些高斯均来自不同输入视角时，该局部区域才被视为有效冗余区域。其有效性指示函数定义为：

$$
m_i =
\mathbb{I}
\left(
\left|
\{s_k \mid k \in \widetilde{\mathcal{N}}_i \cup \{i\}\}
\right|
=
|\widetilde{\mathcal{N}}_i| + 1
\right).
$$

其中，$$\{s_k\}$$ 表示局部候选集合及中心高斯对应的视角来源集合。若集合中高斯的视角来源两两不同，则 $$m_i=1$$；否则 $$m_i=0$$。在此基础上，第 $$i$$ 个高斯的局部冗余分数定义为：

$$
r_i =
m_i
\sum_{j\neq i}
\mathbb{I}
\left(
\|\mathbf{x}_i-\mathbf{x}_j\| < d_i
\;\land\;
\|\mathbf{c}_i-\mathbf{c}_j\| < \delta_c
\right).
$$

该分数衡量了一个高斯附近存在多少位置相近、外观相似且满足跨视角来源约束的候选，因此能够反映该高斯处于多视角重复覆盖区域的程度。相比只根据空间距离计数，颜色约束可以避免将纹理边界、物体边缘或颜色突变区域中的必要高斯误判为冗余项；视角来源约束则进一步保证冗余统计主要来自不同输入视角对同一局部表面的重复预测，而不是同一视角内部的局部高斯密集分布。

随后，我们根据当前高斯集合中所有冗余分数的统计分布，自适应确定冗余阈值：

$$
T = \max(\mu_r + \lambda_r \sigma_r,\ r_{\min}) .
$$

其中，$$\mu_r$$ 和 $$\sigma_r$$ 分别表示当前高斯集合中冗余分数的均值和标准差，$$\lambda_r$$ 控制阈值相对于平均冗余水平的偏移程度，$$r_{\min}$$ 是最小冗余阈值，用于避免低密度区域被误判为冗余。然后，我们将每个高斯的冗余程度归一化为连续系数：

$$
\rho_i =
\operatorname{clip}
\left(
\frac{r_i-T}{r_{\max}-T+\epsilon},
0,1
\right).
$$

其中，$$r_{\max}$$ 表示当前高斯集合中的最大冗余分数，$$\epsilon$$ 用于避免数值不稳定，$$\rho_i\in[0,1]$$ 表示第 $$i$$ 个高斯参与 opacity 调节的程度。当 $$\rho_i=0$$ 时，说明该高斯没有明显的局部重复性，其 opacity 保持不变；当 $$\rho_i$$ 越接近 $$1$$ 时，说明该高斯处于更强的跨视角重复覆盖区域，需要更明显地降低其对最终渲染的叠加贡献。

对满足上述约束的跨视角重复组 $$\mathcal{G}$$，我们进行基于 opacity 的组内重加权。需要强调的是，我们不合并高斯的位置、尺度、旋转或颜色，也不根据距离直接删除高斯，而是只调节其 opacity。设组内第 $$i$$ 个高斯的 opacity 为 $$\alpha_i$$，首先根据组内 opacity 计算其相对贡献权重：

$$
w_i =
\frac{
\exp(\operatorname{logit}(\alpha_i)/\tau)
}{
\sum_{j\in \mathcal{G}}
\exp(\operatorname{logit}(\alpha_j)/\tau)
}.
$$

其中，$$\tau$$ 为温度系数，用于控制组内权重分布的集中程度。较小的 $$\tau$$ 会使权重更多分配给 opacity 较高的高斯，较大的 $$\tau$$ 则得到更加平滑的权重分布。该权重只表示组内高斯之间的相对贡献关系，并不直接替代原始 opacity。

随后，我们根据组内相对权重得到基础缩放项：

$$
\hat{g}_i =
\operatorname{clip}
\left(
|\mathcal{G}|^\beta w_i,\ g_{\min},\ 1
\right).
$$

其中，$$|\mathcal{G}|$$ 表示局部重复组的规模，$$\beta$$ 控制该组整体贡献随候选数量增长的程度，$$g_{\min}$$ 是最小保留系数，用于避免某些高斯被一次性完全压制。在此基础上，我们进一步结合局部冗余系数得到最终 opacity 缩放系数：

$$
g_i = 1-\lambda \rho_i(1-\hat{g}_i).
$$

其中，$$\lambda$$ 控制 opacity 调节强度。最终 opacity 更新为：

$$
\alpha_i' = \alpha_i g_i .
$$

由上述公式可知，opacity 的调节强度同时受到组内相对贡献和局部冗余程度的影响。当某个高斯虽然与其他候选空间接近，但颜色差异明显、局部冗余程度较低，或局部候选不满足跨视角来源约束时，$$\rho_i$$ 接近 $$0$$，此时 $$g_i$$ 接近 $$1$$，其 opacity 基本保持不变；当某个高斯位于多视角重复覆盖区域，并且与周围候选在空间、颜色和视角来源上均满足冗余条件时，$$\rho_i$$ 增大，其 opacity 会根据组内相对贡献权重被重新缩放。这样可以降低多个视角对同一局部表面的重复叠加，同时避免误伤用于表达边缘、纹理或遮挡细节的必要高斯。

这一过程嵌入模型的前向计算，而不是训练后的独立后处理。局部重复关系由三维位置、颜色相似性、输入视角来源和分辨率感知邻域共同确定，不需要额外的可学习分类器；但调整后的 opacity 会继续参与可微渲染和损失计算。因此，渲染误差能够反向影响高斯属性预测，使模型在训练过程中逐渐学习到：在多视角重复覆盖同一表面的区域，并不需要所有候选高斯都保持较高 opacity，而应由更可靠的候选承担主要表达，其他重复候选则降低其有效渲染贡献。
